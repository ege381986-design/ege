# PythonAnywhere için Requirements.txt
# Disk kotası (512 MB) nedeniyle ML kütüphaneleri (torch, transformers, scikit-learn) kaldırıldı

# Temel Flask bileşenleri
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.2
Flask-Mail==0.9.1
Flask-WTF==1.1.1
Werkzeug==2.3.7

# Veri işleme (hafif versiyonlar)
pandas==2.0.3
numpy==1.24.3
requests==2.31.0
openpyxl==3.1.2

# QR kod ve görüntü işleme
qrcode==7.4.2
Pillow==10.0.0

# PDF ve rapor oluşturma
reportlab==4.0.4
python-barcode==0.15.1
matplotlib==3.7.2
seaborn==0.12.2

# Güvenlik ve performans
python-dotenv==1.0.0
Flask-Limiter==3.5.0
flask-caching==2.1.0

# Real-time özellikler
flask-socketio==5.3.4

# Database yönetimi
flask-migrate==4.0.5

# Monitoring (isteğe bağlı)
sentry-sdk[flask]==1.28.1

# OCR (eğer Tesseract kuruluysa)
pytesseract==0.3.10

# Cache (Redis yerine SQLite kullan)
# redis==4.6.0  # PythonAnywhere ücretsizde Redis yok

# Background tasks (Celery yerine basit scheduler kullan)
# celery==5.3.1  # PythonAnywhere ücretsizde background task sınırlı

# Web server (PythonAnywhere kendi WSGI kullanır)
# gunicorn==21.2.0  # Gerek yok

# Diğer yardımcı kütüphaneler
jinja2==3.1.2
itsdangerous==2.1.2
click==8.1.7
blinker==1.6.2
MarkupSafe==2.1.3

# Veritabanı - SQLite varsayılan (PostgreSQL için premium hesap gerekli)
# psycopg2-binary==2.9.7  # Sadece premium hesaplarda
